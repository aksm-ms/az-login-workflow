{
  "name": "az ps test",
  "version": "1.0.0",
  "description": "automate az ps scripts",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^12.0.10",
    "typescript": "^3.5.2"
  },
  "dependencies": {
    "@actions/core": "^1.0.0"
  }
}
