name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run main js
      run: |
        npm install
        node ${{github.workspace}}/main.js
    
