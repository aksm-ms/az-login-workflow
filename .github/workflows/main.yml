name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@master
    - run: |
        powershell -noexit "${{github.workspace}}/test.ps1"
    
    
